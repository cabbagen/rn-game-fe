import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  loadingIcon: {
    marginTop: '40%',
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
  },
  loadingText: {
    marginTop: 8,
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
  }
});

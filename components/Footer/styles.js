import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  footer: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    marginTop: 20,
  },
  footerText: {
    height: 45,
    lineHeight: 45,
    color: '#cccccc',
  }
});
